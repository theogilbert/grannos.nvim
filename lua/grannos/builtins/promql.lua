--- PromQL built-in functions and aggregation operators, for completion
--- and hover.
---
--- GENERATED by scripts/gen_promql_builtins.py from prometheus/prometheus
--- at v3.14.0: promql/parser/functions.go (the function list, argument
--- types, experimental flag), docs/querying/functions.md and
--- docs/querying/operators.md (signatures and descriptions). Do not edit
--- by hand; rerun the script to update.
---
--- @class PromqlBuiltin
--- @field kind         "function"|"aggregation"
--- @field signature    string   as the documentation writes it
--- @field doc          string   one paragraph; empty when the docs say nothing
--- @field experimental boolean  needs --enable-feature=promql-experimental-functions

return {
  --- How every aggregation operator is written, as operators.md states it.
  aggregation_syntax = "<aggr-op> [without|by (<label list>)] ([parameter,] <vector expression>)",

  --- @type table<string, PromqlBuiltin>
  entries = {
    ["abs"] = {
      kind         = "function",
      signature    = "abs(v instant-vector)",
      doc          = "Returns a vector containing all float samples in the input vector converted to their absolute value. Histogram samples in the input vector are ignored silently.",
      experimental = false,
    },
    ["absent"] = {
      kind         = "function",
      signature    = "absent(v instant-vector)",
      doc          = "Returns an empty vector if the vector passed to it has any elements (float samples or histogram samples) and a 1-element vector with the value 1 if the vector passed to it has no elements.",
      experimental = false,
    },
    ["absent_over_time"] = {
      kind         = "function",
      signature    = "absent_over_time(v range-vector)",
      doc          = "Returns an empty vector if the range vector passed to it has any elements (float samples or histogram samples) and a 1-element vector with the value 1 if the range vector passed to it has no elements.",
      experimental = false,
    },
    ["acos"] = {
      kind         = "function",
      signature    = "acos(v instant-vector)",
      doc          = "Calculates the arccosine of all float samples in v (special cases).",
      experimental = false,
    },
    ["acosh"] = {
      kind         = "function",
      signature    = "acosh(v instant-vector)",
      doc          = "Calculates the inverse hyperbolic cosine of all float samples in v (special cases).",
      experimental = false,
    },
    ["asin"] = {
      kind         = "function",
      signature    = "asin(v instant-vector)",
      doc          = "Calculates the arcsine of all float samples in v (special cases).",
      experimental = false,
    },
    ["asinh"] = {
      kind         = "function",
      signature    = "asinh(v instant-vector)",
      doc          = "Calculates the inverse hyperbolic sine of all float samples in v (special cases).",
      experimental = false,
    },
    ["atan"] = {
      kind         = "function",
      signature    = "atan(v instant-vector)",
      doc          = "Calculates the arctangent of all float samples in v (special cases).",
      experimental = false,
    },
    ["atanh"] = {
      kind         = "function",
      signature    = "atanh(v instant-vector)",
      doc          = "Calculates the inverse hyperbolic tangent of all float samples in v (special cases).",
      experimental = false,
    },
    ["avg_over_time"] = {
      kind         = "function",
      signature    = "avg_over_time(range-vector)",
      doc          = "The average value of all float or histogram samples in the specified interval.",
      experimental = false,
    },
    ["ceil"] = {
      kind         = "function",
      signature    = "ceil(v instant-vector)",
      doc          = "Returns a vector containing all float samples in the input vector rounded up to the nearest integer value greater than or equal to their original value. Histogram samples in the input vector are ignored silently.",
      experimental = false,
    },
    ["changes"] = {
      kind         = "function",
      signature    = "changes(v range-vector)",
      doc          = "Returns the number of times its value has changed within the provided time range as an instant vector. A float sample followed by a histogram sample, or vice versa, counts as a change. A counter histogram sample followed by a gauge histogram sample with otherwise exactly the same values, or vice versa, does not count as a change.",
      experimental = false,
    },
    ["clamp"] = {
      kind         = "function",
      signature    = "clamp(v instant-vector, min scalar, max scalar)",
      doc          = "Clamps the values of all float samples in v to have a lower limit of min and an upper limit of max. Histogram samples in the input vector are ignored silently.",
      experimental = false,
    },
    ["clamp_max"] = {
      kind         = "function",
      signature    = "clamp_max(v instant-vector, max scalar)",
      doc          = "Clamps the values of all float samples in v to have an upper limit of max. Histogram samples in the input vector are ignored silently.",
      experimental = false,
    },
    ["clamp_min"] = {
      kind         = "function",
      signature    = "clamp_min(v instant-vector, min scalar)",
      doc          = "Clamps the values of all float samples in v to have a lower limit of min. Histogram samples in the input vector are ignored silently.",
      experimental = false,
    },
    ["cos"] = {
      kind         = "function",
      signature    = "cos(v instant-vector)",
      doc          = "Calculates the cosine of all float samples in v (special cases).",
      experimental = false,
    },
    ["cosh"] = {
      kind         = "function",
      signature    = "cosh(v instant-vector)",
      doc          = "Calculates the hyperbolic cosine of all float samples in v (special cases).",
      experimental = false,
    },
    ["count_over_time"] = {
      kind         = "function",
      signature    = "count_over_time(range-vector)",
      doc          = "The count of all samples in the specified interval.",
      experimental = false,
    },
    ["day_of_month"] = {
      kind         = "function",
      signature    = "day_of_month(v=vector(time()) instant-vector)",
      doc          = "Interprets float samples in v as timestamps (number of seconds since January 1, 1970 UTC) and returns the day of the month (in UTC) for each of those timestamps. Returned values are from 1 to 31. Histogram samples in the input vector are ignored silently.",
      experimental = false,
    },
    ["day_of_week"] = {
      kind         = "function",
      signature    = "day_of_week(v=vector(time()) instant-vector)",
      doc          = "Interprets float samples in v as timestamps (number of seconds since January 1, 1970 UTC) and returns the day of the week (in UTC) for each of those timestamps. Returned values are from 0 to 6, where 0 means Sunday etc. Histogram samples in the input vector are ignored silently.",
      experimental = false,
    },
    ["day_of_year"] = {
      kind         = "function",
      signature    = "day_of_year(v=vector(time()) instant-vector)",
      doc          = "Interprets float samples in v as timestamps (number of seconds since January 1, 1970 UTC) and returns the day of the year (in UTC) for each of those timestamps. Returned values are from 1 to 365 for non-leap years, and 1 to 366 in leap years. Histogram samples in the input vector are ignored silently.",
      experimental = false,
    },
    ["days_in_month"] = {
      kind         = "function",
      signature    = "days_in_month(v=vector(time()) instant-vector)",
      doc          = "Interprets float samples in v as timestamps (number of seconds since January 1, 1970 UTC) and returns the number of days in the month of each of those timestamps (in UTC). Returned values are from 28 to 31. Histogram samples in the input vector are ignored silently.",
      experimental = false,
    },
    ["deg"] = {
      kind         = "function",
      signature    = "deg(v instant-vector)",
      doc          = "Converts radians to degrees for all float samples in v.",
      experimental = false,
    },
    ["delta"] = {
      kind         = "function",
      signature    = "delta(v range-vector)",
      doc          = "Calculates the difference between the first and last value of each time series element in a range vector v, returning an instant vector with the given deltas and equivalent labels. The delta is extrapolated to cover the full time range as specified in the range vector selector, so that it is possible to get a non-integer result even if the sample values are all integers.",
      experimental = false,
    },
    ["deriv"] = {
      kind         = "function",
      signature    = "deriv(v range-vector)",
      doc          = "Calculates the per-second derivative of each float time series in the range vector v, using simple linear regression. The range vector must have at least two float samples in order to perform the calculation. When +Inf or -Inf are found in the range vector, the slope and offset value calculated will be NaN.",
      experimental = false,
    },
    ["double_exponential_smoothing"] = {
      kind         = "function",
      signature    = "double_exponential_smoothing(v range-vector, sf scalar, tf scalar)",
      doc          = "Produces a smoothed value for each float time series in the range in v. The lower the smoothing factor sf, the more importance is given to old data. The higher the trend factor tf, the more trends in the data is considered. Both sf and tf must be between 0 and 1. For additional details, refer to NIST Engineering Statistics Handbook. In Prometheus V2 this function was called holt_winters. This caused confusion since the Holt-Winters method usually refers to triple exponential smoothing. Double exponential smoothing as implemented here is also referred to as \"Holt Linear\".",
      experimental = true,
    },
    ["end"] = {
      kind         = "function",
      signature    = "end()",
      doc          = "Returns the end timestamp of the current query range evaluation as the number of seconds since January 1, 1970 UTC. For instant queries, this is equal to the evaluation timestamp.",
      experimental = true,
    },
    ["exp"] = {
      kind         = "function",
      signature    = "exp(v instant-vector)",
      doc          = "Calculates the exponential function for all float samples in v. Histogram samples are ignored silently. Special cases are:",
      experimental = false,
    },
    ["first_over_time"] = {
      kind         = "function",
      signature    = "first_over_time(range-vector)",
      doc          = "The oldest sample in the specified interval.",
      experimental = false,
    },
    ["floor"] = {
      kind         = "function",
      signature    = "floor(v instant-vector)",
      doc          = "Returns a vector containing all float samples in the input vector rounded down to the nearest integer value smaller than or equal to their original value. Histogram samples in the input vector are ignored silently.",
      experimental = false,
    },
    ["histogram_avg"] = {
      kind         = "function",
      signature    = "histogram_avg(v instant-vector)",
      doc          = "Returns the arithmetic average of observed values stored in each native histogram sample in v. Float samples are ignored and do not show up in the returned vector.",
      experimental = false,
    },
    ["histogram_count"] = {
      kind         = "function",
      signature    = "histogram_count(v instant-vector)",
      doc          = "Returns the count of observations stored in each native histogram sample in v. Float samples are ignored and do not show up in the returned vector.",
      experimental = false,
    },
    ["histogram_fraction"] = {
      kind         = "function",
      signature    = "histogram_fraction(lower scalar, upper scalar, b instant-vector)",
      doc          = "Returns the estimated fraction of observations between the provided lower and upper values for each classic or native histogram contained in b. Float samples in b are considered the counts of observations in each bucket of one or more classic histograms, while native histogram samples in b are treated each individually as a separate histogram. This works in the same way as for histogram_quantile(). (See there for more details.)",
      experimental = false,
    },
    ["histogram_quantile"] = {
      kind         = "function",
      signature    = "histogram_quantile(φ scalar, b instant-vector)",
      doc          = "Calculates the φ-quantile (0 ≤ φ ≤ 1) from a classic histogram or from a native histogram. (See histograms and summaries for a detailed explanation of φ-quantiles and the usage of the (classic) histogram metric type in general.)",
      experimental = false,
    },
    ["histogram_quantiles"] = {
      kind         = "function",
      signature    = "histogram_quantiles(v instant-vector, quantile_label string, φ_1 scalar, φ_2 scalar, ...)",
      doc          = "Calculates multiple (between 1 and 10) φ-quantiles (0 ≤ φ ≤ 1) from a classic histogram or from a native histogram. Quantile calculation works the same way as in histogram_quantile(). The second argument (a string) specifies the label name that is used to identify different quantiles in the query result. histogram_quantiles(sum(rate(foo[1m])), \"quantile\", 0.9, 0.99) # => {quantile=\"0.9\"} 123 {quantile=\"0.99\"} 128",
      experimental = true,
    },
    ["histogram_stddev"] = {
      kind         = "function",
      signature    = "histogram_stddev(v instant-vector)",
      doc          = "Returns the estimated standard deviation of observations for each native histogram sample in v. For this estimation, all observations in a bucket are assumed to have the value of the mean of the bucket boundaries. For the zero bucket and for buckets with custom boundaries, the arithmetic mean is used. For the usual exponential buckets, the geometric mean is used. Float samples are ignored and do not show up in the returned vector.",
      experimental = false,
    },
    ["histogram_stdvar"] = {
      kind         = "function",
      signature    = "histogram_stdvar(v instant-vector)",
      doc          = "Returns the estimated variance of observations for each native histogram sample in v.",
      experimental = false,
    },
    ["histogram_sum"] = {
      kind         = "function",
      signature    = "histogram_sum(v instant-vector)",
      doc          = "Returns the sum of observations stored in each native histogram sample.",
      experimental = false,
    },
    ["hour"] = {
      kind         = "function",
      signature    = "hour(v=vector(time()) instant-vector)",
      doc          = "Interprets float samples in v as timestamps (number of seconds since January 1, 1970 UTC) and returns the hour of the day (in UTC) for each of those timestamps. Returned values are from 0 to 23. Histogram samples in the input vector are ignored silently.",
      experimental = false,
    },
    ["idelta"] = {
      kind         = "function",
      signature    = "idelta(v range-vector)",
      doc          = "Calculates the difference between the last two samples in the range vector v, returning an instant vector with the given deltas and equivalent labels. Both samples must be either float samples or histogram samples. Elements in v where one of the last two samples is a float sample and the other is a histogram sample will be omitted from the result vector, flagged by a warn-level annotation.",
      experimental = false,
    },
    ["increase"] = {
      kind         = "function",
      signature    = "increase(v range-vector)",
      doc          = "Calculates the increase in the time series in the range vector. Breaks in monotonicity (such as counter resets due to target restarts) are automatically adjusted for. The increase is extrapolated to cover the full time range as specified in the range vector selector, so that it is possible to get a non-integer result even if a counter increases only by integer increments.",
      experimental = false,
    },
    ["info"] = {
      kind         = "function",
      signature    = "info(v instant-vector, [data-label-selector instant-vector])",
      doc          = "Finds, for each time series in v, all info series with matching _identifying_ labels (more on this later), and adds the union of their _data_ (i.e., non-identifying) labels to the time series. The second argument data-label-selector is optional. It is not a real instant vector, but uses a subset of its syntax. It must start and end with curly braces ({ ... }) and may only contain label matchers. The label matchers are used to constrain which info series to consider and which data labels to add to v.",
      experimental = true,
    },
    ["irate"] = {
      kind         = "function",
      signature    = "irate(v range-vector)",
      doc          = "Calculates the per-second instant rate of increase of the time series in the range vector. This is based on the last two data points. Breaks in monotonicity (such as counter resets due to target restarts) are automatically adjusted for. Both samples must be either float samples or histogram samples. Elements in v where one of the last two samples is a float sample and the other is a histogram sample will be omitted from the result vector, flagged by a warn-level annotation.",
      experimental = false,
    },
    ["label_join"] = {
      kind         = "function",
      signature    = "label_join(v instant-vector, dst_label string, separator string, src_label_1 string, src_label_2 string, ...)",
      doc          = "Joins all the values of all the src_labels using separator and returns the timeseries with the label dst_label containing the joined value. There can be any number of src_labels in this function.",
      experimental = false,
    },
    ["label_replace"] = {
      kind         = "function",
      signature    = "label_replace(v instant-vector, dst_label string, replacement string, src_label string, regex string)",
      doc          = "Matches the regular expression regex against the value of the label src_label. If it matches, the value of the label dst_label in the returned timeseries will be the expansion of replacement, together with the original labels in the input. Capturing groups in the regular expression can be referenced with $1, $2, etc. Named capturing groups in the regular expression can be referenced with $name (where name is the capturing group name). If the regular expression doesn't match then the timeseries is returned unchanged.",
      experimental = false,
    },
    ["last_over_time"] = {
      kind         = "function",
      signature    = "last_over_time(range-vector)",
      doc          = "The most recent sample in the specified interval.",
      experimental = false,
    },
    ["ln"] = {
      kind         = "function",
      signature    = "ln(v instant-vector)",
      doc          = "Calculates the natural logarithm for all float samples in v. Histogram samples in the input vector are ignored silently. Special cases are:",
      experimental = false,
    },
    ["log10"] = {
      kind         = "function",
      signature    = "log10(v instant-vector)",
      doc          = "Calculates the decimal logarithm for all float samples in v. Histogram samples in the input vector are ignored silently. The special cases are equivalent to those in ln.",
      experimental = false,
    },
    ["log2"] = {
      kind         = "function",
      signature    = "log2(v instant-vector)",
      doc          = "Calculates the binary logarithm for all float samples in v. Histogram samples in the input vector are ignored silently. The special cases are equivalent to those in ln.",
      experimental = false,
    },
    ["mad_over_time"] = {
      kind         = "function",
      signature    = "mad_over_time(range-vector)",
      doc          = "The median absolute deviation of all float samples in the specified interval.",
      experimental = true,
    },
    ["max_of"] = {
      kind         = "function",
      signature    = "max_of(a scalar, b scalar)",
      doc          = "Returns the larger of the two scalar values a and b.",
      experimental = true,
    },
    ["max_over_time"] = {
      kind         = "function",
      signature    = "max_over_time(range-vector)",
      doc          = "The maximum value of all float samples in the specified interval.",
      experimental = false,
    },
    ["min_of"] = {
      kind         = "function",
      signature    = "min_of(a scalar, b scalar)",
      doc          = "Returns the smaller of the two scalar values a and b.",
      experimental = true,
    },
    ["min_over_time"] = {
      kind         = "function",
      signature    = "min_over_time(range-vector)",
      doc          = "The minimum value of all float samples in the specified interval.",
      experimental = false,
    },
    ["minute"] = {
      kind         = "function",
      signature    = "minute(v=vector(time()) instant-vector)",
      doc          = "Interprets float samples in v as timestamps (number of seconds since January 1, 1970 UTC) and returns the minute of the hour (in UTC) for each of those timestamps. Returned values are from 0 to 59. Histogram samples in the input vector are ignored silently.",
      experimental = false,
    },
    ["month"] = {
      kind         = "function",
      signature    = "month(v=vector(time()) instant-vector)",
      doc          = "Interprets float samples in v as timestamps (number of seconds since January 1, 1970 UTC) and returns the month of the year (in UTC) for each of those timestamps. Returned values are from 1 to 12, where 1 means January etc. Histogram samples in the input vector are ignored silently.",
      experimental = false,
    },
    ["pi"] = {
      kind         = "function",
      signature    = "pi()",
      doc          = "Returns pi.",
      experimental = false,
    },
    ["predict_linear"] = {
      kind         = "function",
      signature    = "predict_linear(v range-vector, t scalar)",
      doc          = "Predicts the value of time series t seconds from now, based on the range vector v, using simple linear regression. The range vector must have at least two float samples in order to perform the calculation. When +Inf or -Inf are found in the range vector, the predicted value will be NaN.",
      experimental = false,
    },
    ["present_over_time"] = {
      kind         = "function",
      signature    = "present_over_time(range-vector)",
      doc          = "The value 1 for any series in the specified interval.",
      experimental = false,
    },
    ["quantile_over_time"] = {
      kind         = "function",
      signature    = "quantile_over_time(scalar, range-vector)",
      doc          = "The φ-quantile (0 ≤ φ ≤ 1) of all float samples in the specified interval.",
      experimental = false,
    },
    ["rad"] = {
      kind         = "function",
      signature    = "rad(v instant-vector)",
      doc          = "Converts degrees to radians for all float samples in v.",
      experimental = false,
    },
    ["range"] = {
      kind         = "function",
      signature    = "range()",
      doc          = "Returns the range duration of the current query range evaluation in seconds and is equivalent to end() - start(). For instant queries, this returns 0.",
      experimental = true,
    },
    ["rate"] = {
      kind         = "function",
      signature    = "rate(v range-vector)",
      doc          = "Calculates the per-second average rate of increase of the time series in the range vector. Breaks in monotonicity (such as counter resets due to target restarts) are automatically adjusted for. Also, the calculation extrapolates to the ends of the time range, allowing for missed scrapes or imperfect alignment of scrape cycles with the range's time period.",
      experimental = false,
    },
    ["resets"] = {
      kind         = "function",
      signature    = "resets(v range-vector)",
      doc          = "Returns the number of counter resets within the provided time range as an instant vector. Any decrease in the value between two consecutive float samples is interpreted as a counter reset. A reset in a native histogram is detected in a more complex way: Any decrease in any bucket, including the zero bucket, or in the count of observation constitutes a counter reset, but also the disappearance of any previously populated bucket, a decrease of the zero-bucket width, or any schema change that is not a compatible decrease of resolution.",
      experimental = false,
    },
    ["round"] = {
      kind         = "function",
      signature    = "round(v instant-vector, to_nearest=1 scalar)",
      doc          = "Rounds the sample values of all elements in v to the nearest integer. Ties are resolved by rounding up. The optional to_nearest argument allows specifying the nearest multiple to which the sample values should be rounded. This multiple may also be a fraction. Histogram samples in the input vector are ignored silently.",
      experimental = false,
    },
    ["scalar"] = {
      kind         = "function",
      signature    = "scalar(v instant-vector)",
      doc          = "Returns the sample value of that float sample as a scalar. If the input vector does not have exactly one element with a float sample, scalar will return NaN. Histogram samples in the input vector are ignored silently.",
      experimental = false,
    },
    ["sgn"] = {
      kind         = "function",
      signature    = "sgn(v instant-vector)",
      doc          = "Returns a vector with all float sample values converted to their sign, defined as this: 1 if v is positive, -1 if v is negative and 0 if v is equal to zero. Histogram samples in the input vector are ignored silently.",
      experimental = false,
    },
    ["sin"] = {
      kind         = "function",
      signature    = "sin(v instant-vector)",
      doc          = "Calculates the sine of all float samples in v (special cases).",
      experimental = false,
    },
    ["sinh"] = {
      kind         = "function",
      signature    = "sinh(v instant-vector)",
      doc          = "Calculates the hyperbolic sine of all float samples in v (special cases).",
      experimental = false,
    },
    ["sort"] = {
      kind         = "function",
      signature    = "sort(v instant-vector)",
      doc          = "Returns vector elements sorted by their float sample values, in ascending order. Histogram samples in the input vector are ignored silently.",
      experimental = false,
    },
    ["sort_by_label"] = {
      kind         = "function",
      signature    = "sort_by_label(v instant-vector, label string, ...)",
      doc          = "Returns vector elements sorted by the values of the given labels in ascending order. In case these label values are equal, elements are sorted by their full label sets. sort_by_label acts on float and histogram samples in the same way.",
      experimental = true,
    },
    ["sort_by_label_desc"] = {
      kind         = "function",
      signature    = "sort_by_label_desc(instant-vector, string…)",
      doc          = "**This function has to be enabled via the feature flag --enable-feature=promql-experimental-functions.**",
      experimental = true,
    },
    ["sort_desc"] = {
      kind         = "function",
      signature    = "sort_desc(instant-vector)",
      doc          = "Same as sort, but sorts in descending order.",
      experimental = false,
    },
    ["sqrt"] = {
      kind         = "function",
      signature    = "sqrt(v instant-vector)",
      doc          = "Calculates the square root of all float samples in v. Histogram samples in the input vector are ignored silently.",
      experimental = false,
    },
    ["start"] = {
      kind         = "function",
      signature    = "start()",
      doc          = "Returns the start timestamp of the current query range evaluation as the number of seconds since January 1, 1970 UTC. For instant queries, this is equal to the evaluation timestamp.",
      experimental = true,
    },
    ["start_timestamp"] = {
      kind         = "function",
      signature    = "start_timestamp(v instant-vector)",
      doc          = "Returns the start timestamp of each of the samples of the given vector as the number of seconds since January 1, 1970 UTC. It acts on float and histogram samples in the same way.",
      experimental = true,
    },
    ["stddev_over_time"] = {
      kind         = "function",
      signature    = "stddev_over_time(range-vector)",
      doc          = "The population standard deviation of all float samples in the specified interval.",
      experimental = false,
    },
    ["stdvar_over_time"] = {
      kind         = "function",
      signature    = "stdvar_over_time(range-vector)",
      doc          = "The population variance of all float samples in the specified interval.",
      experimental = false,
    },
    ["step"] = {
      kind         = "function",
      signature    = "step()",
      doc          = "Returns the query resolution step as the number of seconds. For instant queries, this returns 0.",
      experimental = true,
    },
    ["sum_over_time"] = {
      kind         = "function",
      signature    = "sum_over_time(range-vector)",
      doc          = "The sum of all float or histogram samples in the specified interval.",
      experimental = false,
    },
    ["tan"] = {
      kind         = "function",
      signature    = "tan(v instant-vector)",
      doc          = "Calculates the tangent of all float samples in v (special cases).",
      experimental = false,
    },
    ["tanh"] = {
      kind         = "function",
      signature    = "tanh(v instant-vector)",
      doc          = "Calculates the hyperbolic tangent of all float samples in v (special cases).",
      experimental = false,
    },
    ["time"] = {
      kind         = "function",
      signature    = "time()",
      doc          = "Returns the number of seconds since January 1, 1970 UTC. Note that this does not actually return the current time, but the time at which the expression is to be evaluated.",
      experimental = false,
    },
    ["timestamp"] = {
      kind         = "function",
      signature    = "timestamp(v instant-vector)",
      doc          = "Returns the timestamp of each of the samples of the given vector as the number of seconds since January 1, 1970 UTC. It acts on float and histogram samples in the same way.",
      experimental = false,
    },
    ["ts_of_first_over_time"] = {
      kind         = "function",
      signature    = "ts_of_first_over_time(range-vector)",
      doc          = "The timestamp of earliest sample in the specified interval.",
      experimental = true,
    },
    ["ts_of_last_over_time"] = {
      kind         = "function",
      signature    = "ts_of_last_over_time(range-vector)",
      doc          = "The timestamp of last sample in the specified interval.",
      experimental = true,
    },
    ["ts_of_max_over_time"] = {
      kind         = "function",
      signature    = "ts_of_max_over_time(range-vector)",
      doc          = "The timestamp of the last float sample that has the maximum value of all float samples in the specified interval.",
      experimental = true,
    },
    ["ts_of_min_over_time"] = {
      kind         = "function",
      signature    = "ts_of_min_over_time(range-vector)",
      doc          = "The timestamp of the last float sample that has the minimum value of all float samples in the specified interval.",
      experimental = true,
    },
    ["vector"] = {
      kind         = "function",
      signature    = "vector(s scalar)",
      doc          = "Converts the scalar s to a float sample and returns it as a single-element instant vector with no labels.",
      experimental = false,
    },
    ["year"] = {
      kind         = "function",
      signature    = "year(v=vector(time()) instant-vector)",
      doc          = "Returns the year for each of the given times in UTC. Histogram samples in the input vector are ignored silently.",
      experimental = false,
    },
    ["avg"] = {
      kind         = "aggregation",
      signature    = "avg(v)",
      doc          = "Calculate the arithmetic average over dimensions",
      experimental = false,
    },
    ["bottomk"] = {
      kind         = "aggregation",
      signature    = "bottomk(k, v)",
      doc          = "Smallest k elements by sample value",
      experimental = false,
    },
    ["count"] = {
      kind         = "aggregation",
      signature    = "count(v)",
      doc          = "Count number of elements in the vector",
      experimental = false,
    },
    ["count_values"] = {
      kind         = "aggregation",
      signature    = "count_values(l, v)",
      doc          = "Count number of elements with the same value",
      experimental = false,
    },
    ["group"] = {
      kind         = "aggregation",
      signature    = "group(v)",
      doc          = "All values in the resulting vector are 1",
      experimental = false,
    },
    ["limit_ratio"] = {
      kind         = "aggregation",
      signature    = "limit_ratio(r, v)",
      doc          = "Sample a pseudo-random ratio r of elements",
      experimental = true,
    },
    ["limitk"] = {
      kind         = "aggregation",
      signature    = "limitk(k, v)",
      doc          = "Sample k elements",
      experimental = true,
    },
    ["max"] = {
      kind         = "aggregation",
      signature    = "max(v)",
      doc          = "Select maximum over dimensions",
      experimental = false,
    },
    ["min"] = {
      kind         = "aggregation",
      signature    = "min(v)",
      doc          = "Select minimum over dimensions",
      experimental = false,
    },
    ["quantile"] = {
      kind         = "aggregation",
      signature    = "quantile(φ, v)",
      doc          = "Calculate φ-quantile (0 ≤ φ ≤ 1) over dimensions",
      experimental = false,
    },
    ["stddev"] = {
      kind         = "aggregation",
      signature    = "stddev(v)",
      doc          = "Calculate population standard deviation over dimensions",
      experimental = false,
    },
    ["stdvar"] = {
      kind         = "aggregation",
      signature    = "stdvar(v)",
      doc          = "Calculate population variance over dimensions",
      experimental = false,
    },
    ["sum"] = {
      kind         = "aggregation",
      signature    = "sum(v)",
      doc          = "Calculate sum over dimensions",
      experimental = false,
    },
    ["topk"] = {
      kind         = "aggregation",
      signature    = "topk(k, v)",
      doc          = "Largest k elements by sample value",
      experimental = false,
    },
  },
}
